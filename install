#!/usr/bin/env sh

set -eu

repo_dir=$(CDPATH= cd -- "$(dirname "$0")" && pwd)
mkdir -p "$HOME/.config/mise"

for file in zshrc zshenv gitignore inputrc vimrc gitconfig
do
  ln -sfn "$repo_dir/$file" "$HOME/.$file"
done

ln -sfn "$repo_dir/.config/mise/config.toml" "$HOME/.config/mise/config.toml"
